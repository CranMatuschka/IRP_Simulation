{
  "_id": "feat rung: noIonosphere. One toggle against golden_baseline.json; everything else is inherited via _extends, so the difference in the result is attributable to this toggle alone. Ionosphere OFF in truth and model. Removes the whole slant-delay chain.",
  "_extends": "golden_baseline.json",
  "_delta": "Ionosphere OFF in truth and model. Removes the whole slant-delay chain.",
  "scenario": {
    "name": "feat002_noIonosphere",
    "nTowers": 5,
    "nSpaceAssets": 1,
    "nReceivers": 4
  },
  "_pairNote": "The truth/model pair is written out explicitly, not left to the master enable -- see feat001. Higher-order needs no leaf here: higherOrderIono_ derives from the first-order slant delay, which is zero once truth.enable is false. Scintillation is NOT switched off here -- it is gated independently of the slant-delay chain and owns its own rung (feat005), so the ladder axes stay orthogonal.",
  "errors": {
    "ionosphere": {
      "enable": false,
      "truth": {
        "enable": false
      },
      "model": {
        "enable": false
      }
    }
  },
  "report": {
    "runVersion": "feat002",
    "compileTex": "auto"
  },
  "_reportNote": "compileTex 'auto' rather than the golden's 'require': a ladder rung's deliverable is its .mat, and a pdflatex hiccup must not throw away a finished 25-minute run. ReportRunner only escalates a failed compile to an error under 'require'."
}
