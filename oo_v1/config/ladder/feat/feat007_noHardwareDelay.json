{
  "_id": "feat rung: noHardwareDelay. One toggle against golden_baseline.json; everything else is inherited via _extends, so the difference in the result is attributable to this toggle alone. Per-tower hardware group delay OFF, including its residual process.",
  "_extends": "golden_baseline.json",
  "_delta": "Per-tower hardware group delay OFF, including its residual process.",
  "scenario": {
    "name": "feat007_noHardwareDelay",
    "nTowers": 5,
    "nSpaceAssets": 1,
    "nReceivers": 4
  },
  "_pairNote": "The truth/model pair is written out explicitly, not left to the master enable -- see feat001. ErrorChain.hardwareDelay_ documents at :749-757 that it DELIBERATELY never reads the master, so the pair is the only switch that works here. truth.enable=false also closes the stochastic residual channel and its R contribution (both are gated on truth.enable at :787-793).",
  "errors": {
    "hardwareDelay": {
      "enable": false,
      "truth": { "enable": false },
      "model": { "enable": false }
    }
  },
  "report": {
    "runVersion": "feat007"
  }
}
