{
  "_id": "feat rung: noTowerSurvey. One toggle against golden_baseline.json; everything else is inherited via _extends, so the difference in the result is attributable to this toggle alone. Truth-only tower survey error OFF (the model already assumes perfect coordinates, so this removes a pure truth-model mismatch).",
  "_extends": "golden_baseline.json",
  "_delta": "Truth-only tower survey error OFF (the model already assumes perfect coordinates, so this removes a pure truth-model mismatch).",
  "scenario": {
    "name": "feat009_noTowerSurvey",
    "nTowers": 5,
    "nSpaceAssets": 1,
    "nReceivers": 4
  },
  "_pairNote": "The truth/model pair is written out explicitly, not left to the master enable -- see feat001. MeasurementModelUtils.towerPositionEcef reads ts.(side).enable only; the master reaches no consumer.",
  "effects": {
    "towerSurvey": {
      "enable": false,
      "truth": {
        "enable": false
      },
      "model": {
        "enable": false
      }
    }
  },
  "report": {
    "runVersion": "feat009",
    "compileTex": "auto"
  },
  "_reportNote": "compileTex 'auto' rather than the golden's 'require': a ladder rung's deliverable is its .mat, and a pdflatex hiccup must not throw away a finished 25-minute run. ReportRunner only escalates a failed compile to an error under 'require'."
}
