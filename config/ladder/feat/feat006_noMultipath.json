{
  "_id": "feat rung: noMultipath. One toggle against golden_baseline.json; everything else is inherited via _extends, so the difference in the result is attributable to this toggle alone. Coloured code multipath OFF (truth-only in the golden, 60 s correlated).",
  "_extends": "golden_baseline.json",
  "_delta": "Coloured code multipath OFF (truth-only in the golden, 60 s correlated).",
  "scenario": {
    "name": "feat006_noMultipath",
    "nTowers": 5,
    "nSpaceAssets": 1,
    "nReceivers": 4
  },
  "_pairNote": "The truth/model pair is written out explicitly, not left to the master enable -- see feat001. ErrorChain.multipath_ gates the coloured Gauss-Markov branch on truth.enable && coloredGM.enable and the legacy sinusoid on truth.enable alone, so truth.enable=false closes both.",
  "errors": {
    "multipath": {
      "enable": false,
      "truth": {
        "enable": false
      },
      "model": {
        "enable": false
      }
    }
  },
  "report": {
    "runVersion": "feat006",
    "compileTex": "auto"
  },
  "_reportNote": "compileTex 'auto' rather than the golden's 'require': a ladder rung's deliverable is its .mat, and a pdflatex hiccup must not throw away a finished 25-minute run. ReportRunner only escalates a failed compile to an error under 'require'."
}
